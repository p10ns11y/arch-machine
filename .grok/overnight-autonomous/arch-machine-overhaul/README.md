# Arch-Machine Overhaul — Self-Referential Autonomous Handoff

This folder is the **living, executable, sentinel-grade control plane** for the full arch-machine repository and automation overhaul.

**Created**: 2026-05-29 (in response to `arch-machine-overhaul-context-for-grok-build.md` + user directive for highest security seniority self-referential work loop).

**Mindset**: Stellarfusion (grand-scale integrative fusion of the project's vigilant security DNA, ML/AI reproducibility, evidence self-loops, elegant automation, and preserved humor) applied at the **highest security seniority level** (principal security architect + ML platform expert rigor).

## Contents
- `CONTEXT.md` — Complete self-contained mission briefing (synthesized from the provided overhaul context + exhaustive exploration findings).
- `INIT_PLAN.md` — Detailed 6-phase autonomous execution plan (self-updating, with mandatory self-referential instructions, policy application, evidence production, and branch discipline baked in).
- `STATE.md` — Live current truth (read first, write last; checkpoints, open items, evidence refs).
- `PROGRESS.md` — Append-only narrative history (with SHAs, evidence bundle links, lessons).
- `EVIDENCE/` — Subdirectory for the overhaul's own AI-optimized evidence bundles (produced and consumed by the agent at every phase gate).

## How the Agent Uses This Handoff
1. Start on a proper hotfix/ branch (hotfix/arch-machine-overhaul-* or vir tinel-style).
2. Before **every** major action or phase: Re-read all four core files + run the project's own `tinfoil` (thin) + `maintenance/security-audit.sh` + `maintenance/extract-evidence.sh` on the current tree. Commit the resulting evidence bundle.
3. Update living STATE.md and PROGRESS.md after every step.
4. Apply the project's own `policies/security-remediation.md` ruthlessly to every "vulnerable" artifact touched (duplication/legacy = high-severity vuln → audit → fix/kill after evidence).
5. All work on hotfix/feature branches only. PRs to protected `sentinel` only at phase gates.
6. `install.sh` public interface must remain functional after every change.
7. The handoff folder itself is a first-class, evidence-producing citizen of the platform.

## Relationship to Prior Work
- Superseded the narrower `cli-policy-remediation-tui/` handoff (TUI + policy remediation, 2026-05-28). That folder was **deleted** in the 2026-07 hard pass (SO-6); this overhaul folder remains the historical archive.
- Gum-as-primary decision is obsolete — live control plane is `tools/archy` (see `docs/archy.md`).

## Success
When the 6 phases are complete, the repo will be a model citizen of its own principles: clean, duplication-free, professionally surfaced, with the evidence self-loop amplified and the platform's own security-remediation policy applied to its own technical debt.

**This is the complete handoff.** The autonomous agent (Grok Build or future) can now take over and execute at the highest security seniority level with no further input needed.

The Vigilant Guardian approves. 🛡️