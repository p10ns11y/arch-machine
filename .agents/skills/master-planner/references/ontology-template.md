# Ontology template (large / complex projects)

```text
.agents/ontology/
├── INDEX.md
├── <project>.graph.yaml
└── GRAPH.md
```

Load **one subgraph** per task. IDs stable (`proj:Name`). `source_refs` must exist.
