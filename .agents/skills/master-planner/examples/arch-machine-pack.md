# Pack — arch-machine (arch-guardian)

**Mission:** Self-remediating Arch guardian — Eagle routes; shells act; evidence closes.

| Skill | Source |
|-------|--------|
| master-planner | in-repo `.agents/skills/master-planner` |
| eagle-satellite-elomaxz | in-repo |
| session-unit-order | in-repo |
| catalog pack | `npx skills add p10ns11y/skills -s …` → `skills-lock.json` |

**Overlays:** `.agents/overlays/arch-machine-*.md`  
**Ontology:** `.agents/ontology/`

**Restore:**

```bash
npx skills experimental_install
```
