# groxy allowlist — only these X users may remote-control this host via XChat DM.
# Prefer numeric user ids (stable). Usernames are lowercased without @.
#
# Operator: set your own id (xurl /2/users/me → data.id)
# Example for Peramanathan (this machine's primary operator):

allowlist_ids=295441607
allowlist_usernames=Peramanathan

# High-blast verbs (pkg/install/rm/…) require: confirm <token> …
require_confirm=true
